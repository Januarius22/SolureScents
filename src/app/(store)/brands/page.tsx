import type { Metadata } from "next"; import { getBrands } from "@/features/storefront/services/storefront-service";
export const metadata:Metadata={title:"Fragrance houses",description:"Discover the perfume houses represented by Solure Scents."};
export default async function BrandsPage(){const brands=await getBrands();return <div className="mx-auto max-w-[90rem] px-5 py-20 sm:px-8 lg:px-12"><p className="text-xs tracking-[.2em] text-gold uppercase">The houses</p><h1 className="mt-4 font-display text-6xl">Brands</h1><div className="mt-12 grid border-t sm:grid-cols-2 lg:grid-cols-3">{brands.length?brands.map(b=><article className="border-b p-8 sm:border-r" key={b.id}><p className="text-xs text-gold">{b.country_code ?? "International"}</p><h2 className="mt-5 font-display text-3xl">{b.name}</h2><p className="mt-3 text-sm leading-6 text-muted">{b.description ?? "A fragrance house selected for its distinct point of view."}</p></article>):<p className="col-span-full py-20 text-muted">Our fragrance-house directory is being curated.</p>}</div></div>}
