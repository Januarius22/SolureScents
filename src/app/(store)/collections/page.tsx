import type { Metadata } from "next"; import Link from "next/link"; import { getCollections } from "@/features/storefront/services/storefront-service";
export const metadata:Metadata={title:"Collections"}; export default async function CollectionsPage(){const items=await getCollections();return <div className="mx-auto max-w-[90rem] px-5 py-20 sm:px-8 lg:px-12"><p className="text-xs tracking-[.2em] text-gold uppercase">Curated moods</p><h1 className="mt-4 font-display text-6xl">Collections</h1><div className="mt-12 grid gap-6 md:grid-cols-2">{items.length?items.map((x,i)=><Link href={`/shop?collection=${x.id}`} className="group min-h-80 bg-ivory p-10 sm:p-14" key={x.id}><span className="text-xs text-gold">0{i+1}</span><h2 className="mt-20 font-display text-4xl group-hover:text-gold">{x.name}</h2><p className="mt-4 max-w-md text-muted">{x.description ?? "An edited expression of scent and memory."}</p></Link>):<p className="py-20 text-muted">Our first collections are being composed.</p>}</div></div>}
