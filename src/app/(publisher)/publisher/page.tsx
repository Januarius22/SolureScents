import { SectionHeading } from "@/components/ui/section-heading";

export const metadata = { title: "Publishing Studio" };

/** Publisher application boundary confirmation page. */
export default function PublisherPage() {
  return <SectionHeading eyebrow="Editorial" title="Publishing Studio" />;
}
