import { AccountHeader, Panel, buttonClass, inputClass } from "@/components/account/account-ui";
import { updateProfile } from "@/features/account/actions/account-actions";
import { getAccountData } from "@/features/account/services/account-service";
export const metadata={title:"Profile"};
export default async function Page(){const {access,profile}=await getAccountData();return <><AccountHeader eyebrow="Personal details" title="Profile" copy="Keep your contact details accurate for a seamless concierge experience."/><Panel><form action={updateProfile} className="max-w-xl space-y-5"><label className="block text-xs font-semibold tracking-wider uppercase">Full name<input className={`${inputClass} mt-2`} defaultValue={profile.full_name??""} name="full_name" required/></label><label className="block text-xs font-semibold tracking-wider uppercase">Phone<input className={`${inputClass} mt-2`} defaultValue={profile.phone??""} name="phone" required type="tel"/></label><label className="block text-xs font-semibold tracking-wider uppercase">Email<input className={`${inputClass} mt-2 bg-stone-50 text-muted`} disabled value={access.email}/></label><button className={buttonClass} type="submit">Save profile</button></form></Panel></>}
